description: Download a recalibration VCF file from the GATK FTP server and perform some preprocessing so the VCF file will work properly, even if the reference genome was sorted differently than the VCF file.
args:
    - VCF_URL:
        default: "Null"
        opts: "-u | --vcf_url"
        description: "URL for a recalibration VCF file from the GATK VCF file."
        example: "-u ftp://gsapubftp-anonymous@ftp.broadinstitute.org/bundle/hg38/dbsnp_146.hg38.vcf.gz"
    - FASTA_FILE:
        default: "Null"
        opts: "-r | --reference"
        description: "Name of the reference genome FASTA file."
        example: "-r hg19.fa"
    - OUTPUT_VCF_FILE:
        default: "Null"
        opts: "-o | --output"
        description: "Name of the output VCF file."
        example: "-o dbsnp_146.hg38.vcf.gz"
volumes:
    - ref_dir:
        description: "Directory where the reference genome FASTA file and indices are stored."
    - output_vcf_dir:
        description: "Directory where the output VCF file will be stored."
        write_access: True
command_template: |
    VCF_FILE=/tmp/"$(basename ${VCF_URL/\.gz/})" \
    TMP_FILE=/tmp/"$(basename ${VCF_FILE}).2" \
    OUT_FILE=/data/output_vcf_dir/"$(basename ${OUTPUT_VCF_FILE})"

    wget -O "${VCF_FILE}.gz" "${VCF_URL}"
    gunzip "${VCF_FILE}.gz"

    DICT_FILE=/data/ref_dir/${FASTA_FILE/\.$(get_file_extension ${FASTA_FILE})/}.dict
    python /starling/helper/reconcile_vcf_with_dict.py "${VCF_FILE}" "${DICT_FILE}" "${TMP_FILE}"

    picard -Xms512m -Xmx8g SortVcf \
        I="${TMP_FILE}" \
        O="${OUT_FILE}" \
        SEQUENCE_DICTIONARY="${DICT_FILE}" \
        CREATE_INDEX=true
receipt_commands: |
    get_bash_version
    wget --version
    gzip --version
    get_python_version
    get_conda_version picard