description: Create a pileup file using samtools. This file shows differences between a normal BAM file and a tumor BAM file.
args:
    - FASTA_FILE:
        default: "Null"
        opts: "-r | --reference"
        description: "Name of the reference genome FASTA file."
        example: "-r hg19.fa"
    - TUMOR_BAM_FILE:
        default: "Null"
        opts: "-t | --tumor_bam_file"
        description: "Path to a BAM file with tumor data."
        example: "-t SampleA_Tumor.bam"
    - NORMAL_BAM_FILE:
        default: "Null"
        opts: "-n | --normal_bam_file"
        description: "Path to a BAM file with normal data."
        example: "-n SampleA_Normal.bam"
    - OUTPUT_FILE:
        default: "Null"
        opts: "-o | --output"
        description: "Name of the output file."
        example: "-o SampleA.pileup"
volumes:
    - ref_dir:
        description: "Directory where the reference genome FASTA file and indices are stored."
        permissions: Read
    - input_bam_dir:
        description: "Directory where the BAM file is stored."
        permissions: Read
    - output_pileup_dir:
        description: "Directory where the output file will be stored."
        permissions: ReadWrite
command_template: |
    ln -s /data/ref_dir/"${FASTA_FILE}" /tmp/"${FASTA_FILE}"

    # Check for necessary index files in ref_index directory

    INDEX=$(echo ${FASTA_FILE} | grep -o '\.' | grep -c '\.')
    if [[ ${FASTA_FILE: -${INDEX}} = ".gz" ]]; then
        NEW_REF="$(echo ${FASTA_FILE} | cut -d '.' -f -${INDEX})"
        gunzip -c /data/ref_dir/"${FASTA_FILE}" > /tmp/"${NEW_REF}"
        FASTA_FILE="${NEW_REF}"
    fi

    NEEDED_FILE=/data/ref_dir/${FASTA_FILE}.fai

    if [[ ! -f "${NEEDED_FILE}" ]]; then
        echo "Samtools reference index (${NEEDED_FILE}) is missing."
        exit 1
    fi

    ln -s /data/ref_dir/"${FASTA_FILE}.fai" /tmp/"${FASTA_FILE}.fai"

    samtools mpileup -B -q 1 -f /tmp/"${FASTA_FILE}" \
      /data/input_bam_dir/"${NORMAL_BAM_FILE}" /data/input_bam_dir/"${TUMOR_BAM_FILE}" > /data/output_pileup_dir/"${OUTPUT_FILE}"
receipt_commands: |
    get_bash_version
    get_conda_version samtools